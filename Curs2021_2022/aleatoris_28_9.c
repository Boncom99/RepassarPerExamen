#include <stdio.h>
#include <stdlib.h>
#include <time.h>
//srand(4) per obtenir sempre els mateixos randoms
//srand(time(NULL)) va canviant. és el nombre de milisegons desdel 1/1/1970
//rand() -> num aleatori enter [0, RAND_MAX=32767].
//si volem rand() entre 0 i 1 fem:
//(double)rand()/RAND_MAX;
//(double perque no sigui divisió enteras)

int main()
{

    return 0;
}